
Сделали коммит. Чтобы доставить в удалённую репу:
```bash
git push
```

## Fast-Forward

Чтобы обычный push сработал, должно выполняться условие fast-forward.

## Force push

Нужен, чтобы переписать историю коммитов (например, после `rebase`)