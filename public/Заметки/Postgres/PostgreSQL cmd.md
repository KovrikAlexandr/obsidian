- Подключение к базе через консоль:
```bash
psql -h <hostname/ip> -p <port> -d <database> -U <username>
```

## Команды внутри утилиты
Дописать


```bash
psql -h localhost -p 5432 -d postgres -U postgres
```