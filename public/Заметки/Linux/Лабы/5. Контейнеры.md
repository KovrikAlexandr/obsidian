## 1. Квоты на процессор для конкретного пользователя (cgroups v2)
- Создайте пользователя: user-ID (например, user-72):

```bash
useradd user-4
```

- Назначьте квоту процессора 30% CPU:

```bash
mkdir -p /sys/fs/cgroup/user.slice/user-4
echo "30000 100000" > cpu.max
```

![[Pasted image 20260404021229.png]]

## 2. Ограничение памяти для процесса (cgroups)
- Создайте cgroup для ограничения памяти, потребляемой процессом.
- Запустите процесс и переместите его в созданную вами группу.
- Пример команды: `tail /dev/zero`.
- Ограничьте потребление памяти следующим образом: 540 МБ
- Проверьте, что при исчерпании памяти процессом он прерывается ОС

```bash
cgcreate -g memory:/mem_limit_group
cgset -r memory.max=540M mem_limit_group
cgexec -g memory:mem_limit_group tail /dev/zero
dmesg | grep -i oom
```
![[Pasted image 20260404022651.png]]

## 3. Ограничение дискового ввода-вывода для сценария резервного копирования (cgroups)
• Скрипт резервного копирования (backup.sh) перегружает дисковую подсистему.
• Ограничьте его до:
	◦ Чтение: 1040 IOPS
	◦ Запись: 540 IOPS
• Используйте cgcreate для установки ограничений io.max.
• Протестируйте с помощью fio

```bash
cgcreate -g io:backup
cgset -r io.max="253:17 riops=1040 wiops=540" backup
```

Тест на чтение:
```bash
cgexec -g io:backup fio --name=read_test --filename=/tmp/fio.test --ioengine=libaio --iodepth=1 --rw=randread --bs=4k --direct=1 --numjobs=1 --time_based --runtime=10s --size=1G
```
![[Pasted image 20260404024726.png]]

Тест на запись:
```bash
cgexec -g io:backup fio --name=write_test --filename=/tmp/fio.test --ioengine=libaio --iodepth=1 --rw=randwrite --bs=4k --direct=1 --numjobs=1 --time_based --runtime=10s --size=1G
```
![[Pasted image 20260404024750.png]]

## 4. Закрепление к определенному ядру процессора для приложения
- Настройте с помощью cgroups процесс команды top за процессором 0
- Используйте cpuset.cpus в cgroups
- Проверьте с помощью taskset -p PID

```bash
cgcreate -g cpuset:/i_want_to_sleep_please
cgset -r cpuset.cpus=0 i_want_to_sleep_please
cgexec -g cpuset:i_want_to_sleep_please top -b > /dev/null &
taskset -cp 2801
```
![[Pasted image 20260404025216.png]]

## 5. Динамическая корректировка ресурсов (cgroups)
- Напишите сценарий для мониторинга нагрузки по CPU и динамического изменения cpu.max определенного процесса (его идентификатор задается как входной параметр скрипта).
- Квота ЦП для процесса должна регулироваться в зависимости от общей нагрузки на систему:
	- Низкая нагрузка (CPU < 20%): 80% CPU.
	- Высокая нагрузка (CPU > 60%): 30% CPU.

Скрипт:
```bash
#!/bin/bash

PID="$1"
GROUP="cpu_monitor"

cgcreate -g cpu:/$GROUP
cgclassify -g cpu:/$GROUP "$PID"

while true; do
    read cpu user nice system idle iowait irq softirq steal guest guest_nice < /proc/stat
    total1=$((user + nice + system + idle + iowait + irq + softirq + steal))
    idle1=$idle

    sleep 2

    read cpu user nice system idle iowait irq softirq steal guest guest_nice < /proc/stat
    total2=$((user + nice + system + idle + iowait + irq + softirq + steal))
    idle2=$idle

    total_diff=$((total2 - total1))
    idle_diff=$((idle2 - idle1))
    usage=$((100 * (total_diff - idle_diff) / total_diff))

    if [ "$usage" -lt 20 ]; then
        cgset -r cpu.max="80000 100000" "$GROUP"
    elif [ "$usage" -gt 60 ]; then
        cgset -r cpu.max="30000 100000" "$GROUP"
    fi

    echo "CPU load: ${usage}% | cpu.max=$(cat /sys/fs/cgroup/$GROUP/cpu.max)"
done
```

Проверка:
![[Pasted image 20260404123951.png]]

## 6. Создание изолированного имени хоста (пространство имен UTS)
- Запустите оболочку (shell/bash) в отдельном namespace, в которой можно изменить имя хоста, не затрагивая хост.
- Измените имя хоста внутри пространства имен на isolated-student-04.
- Проверьте изоляцию:
- hostname # Должно отображаться «isolated-student-».
- Проверьте имя хоста (в новом терминале): hostname # По-прежнему показывает оригинальное имя хоста.

```bash
unshare --uts --fork bash 
hostnamectl isolated-student04 
hostname
```
![[Pasted image 20260404030346.png]]

## 7. Изоляция процессов (пространство имен PID)

```bash
unshare --pid --fork bash
mount -t proc proc /proc
ps aux
```

![[Pasted image 20260404030718.png]]


## 8. Изолированная файловая система (пространство имен Mount)

```bash
unshare --mount bash
mkdir -p /tmp/private_$(whoami)
mount -t tmpfs tmpfs /tmp/private_$(whoami)
df -h | grep private_$(whoami)
```
![[Pasted image 20260404032043.png]]


## 9. Отключение доступа к сети (пространство имен Network)

```bash
unshare --net bash
ip a show
ping google.com
```
![[Pasted image 20260404032210.png]]


## 10. Создайте и проанализируйте монтирование OverlayFS

1. Первоначальная настройка:
2. Имитация неполадки и отладка:
	- Удалите файл 04_original.txt из каталога merged.
	- Понаблюдайте: Какой файл(ы) появился(ись) в верхнем каталоге? Задокументируйте их имена и содержимое.
	- Измените каталог merged, чтобы восстановить 04_original.txt, не размонтируя и не изменяя нижний уровень.

```bash
mkdir -p ~/overlay/{lower,upper,work,merged}
echo "Оригинальный текст из LOWER" > ~/overlay/lower/04_original.txt

mount -t overlay overlay -o lowerdir=/root/overlay/lower,upperdir=/root/overlay/upper,workdir=/root/overlay/work /root/overlay/merged

rm overlay/merged/04_original.txt
ls -l ~/overlay/upper

rm -f upper/04_original.txt && cp lower/04_original.txt merged/
```
![[Pasted image 20260404122354.png]]


c. Разработайте скрипт, который:
• Обнаруживает все whiteout файлы в верхнем каталоге upper.
• Сравнивает содержимое нижнего и объединенного для выявления несоответствий.
• Выводит отчет с именем 04_audit.log.

```bash
#!/bin/bash

BASE=~/overlay
LOWER="$BASE/lower"
UPPER="$BASE/upper"
MERGED="$BASE/merged"
LOG=04_audit.log

{
    echo "WHITEOUT FILES:"
    find "$UPPER" \( -name '.wh.*' -o -type c \) | while read f; do
        ls -l "$f"
    done

    echo
    echo "FILES ONLY IN LOWER:"
    find "$LOWER" -type f | sed "s|^$LOWER/||" | sort > /tmp/lower_list.txt
    find "$MERGED" -type f | sed "s|^$MERGED/||" | sort > /tmp/merged_list.txt
    comm -23 /tmp/lower_list.txt /tmp/merged_list.txt

    echo
    echo "FILES WITH DIFFERENT CONTENT:"
    for f in $(find "$LOWER" -type f | sed "s|^$LOWER/||"); do
        if [ -f "$MERGED/$f" ]; then
            cmp -s "$LOWER/$f" "$MERGED/$f" || echo "$f"
        fi
    done
} > "$LOG"
```
![[Pasted image 20260404123018.png]]


d. Ответьте на вопросы:
- Как OverlayFS скрывает файлы из нижнего слоя при удалении в объединенном?
	Созданием whiteout файла в upper

- Если вы удалите рабочий каталог work, сможете ли вы перемонтировать оверлей Объясните, почему.
	Перемонтировать overlay не получится. Он используется ядром для copy-on-write, временных и атомарных операций

- Что произойдет с объединенным слоем, если верхний каталог будет пуст?
	Будет искать файл в lower

## 11. Оптимизируйте Dockerfile для приведенного ниже приложения app.py

![[Pasted image 20260404032357.png]]

Фиксируем зависимости:
```bash
pip freeze > requirements.txt
```

Плохой:
```Dockerfile
FROM python:latest
COPY . /app
WORKDIR /app
RUN pip install flask
CMD ["python", "app.py"]
```

Оптимизированный:
```Dockerfile
FROM python:3.9-slim

RUN useradd -f flask && mkdir -p /app
WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY --chown flask:flask app.py .

USER flask

CMD ["python", "app.py"]
```

dockerignore:
```
.git
__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store
.env
venv
```


## 12. Установка платформы публикации WordPress с помощью Docker Compose

Композина:

```yml
services:
  wordpress:
    depends_on:
      - db
    image: dhi.io/wordpress:6.9.4-debian13-php8.5-fpm-dev
    restart: unless-stopped
    environment:
      WORDPRESS_DB_HOST: db
      WORDPRESS_DB_USER: user
      WORDPRESS_DB_PASSWORD: password
      WORDPRESS_DB_NAME: db_name
    ports:
      - "2004:80"
    volumes:
      - wp-data:/var/www/html
  db:
    container_name: db
    image: mariadb:10.6.25-jammy
    restart: unless-stopped
    environment:
      - MYSQL_ROOT_PASSWORD=password
      - MYSQL_DATABASE=db_name
      - MYSQL_USER=user
      - MYSQL_PASSWORD=password
    volumes:
      - db-data:/var/lib/mysql

volumes:
	wp-data:
	db-data: 
```