1. На первом добавленном диске создайте новый раздел, начинающийся с первого свободного сектора и имеющий размер 500 МБайт.
```bash
sudo fdisk /dev/vdb # Или любой другой диск
# g – создать GPT табличку
# n – новый раздел
# Все дефолты, последний сектор выбираем +500M
```
![[images/Pasted image 20260305000103.png]]


Смотрим, что получилось командой `p` и командой `w` делаем запись
![[images/Pasted image 20260305000346.png]]

2. Создайте файл в домашнем каталоге пользователя root и сохраните в него UUID созданного раздела:
```bash
lsblk -o NAME,PARTUUID,UUID --json | jq '.blockdevices[1].children[0].partuuid' > /root/partuuid.txt
```
![[images/Pasted image 20260305001134.png]]

3. Создайте на созданном разделе файловую систему ext4 с размером блока 4096 байт:
```bash
sudo mkfs.ext4 -b 4k /dev/vdb1
```
![[images/Pasted image 20260305001539.png]]

4. Выведите на экран текущее состояние параметров, записанных в суперблоке созданной файловой системы.
```bash
sudo dumpe2fs /dev/vdb1
```
![[images/Pasted image 20260305002019.png]]

5. Настройте эту файловую систему таким образом, чтобы ее автоматическая проверка запускалась через 2 месяца или каждое второе монтирование файловой системы.
```bash
sudo tune2fs -c 2 -i 2m /dev/vdb1
```
![[images/Pasted image 20260305002753.png]]

6. Создайте в каталоге /mnt подкаталог newdisk и подмонтируйте в него созданную файловую систему.
```bash
sudo mkdir -p /mnt/newdisk
sudo mount /dev/vdb1 /mnt/newdisk
```
![[images/Pasted image 20260305003158.png]]

7. Создайте в домашнем каталоге пользователя root ссылку на смонтированную файловую систему:
```bash
cd /root
sudo ln -s /mnt/newdisk link
```
![[images/Pasted image 20260305003518.png]]
PS: hard link на каталоги нельзя, поэтому soft link

8. Создайте каталог с любым именем в смонтированной файловой системе.
9. Включите автомонтирование при запуске операционной системы созданной файловой системы в /mnt/newdisk таким образом, чтобы
	- было невозможно запускать исполняемые файлы, находящиеся в этой системе
	- с отключением возможности записи времени последнего доступа к файлу для ускорения работы с этой файловой системой
Перезагрузите операционную систему и проверьте доступность файловой системы. Проверьте невозможность запустить исполняемый файл, если он хранится в этой файловой системе.
![[images/Pasted image 20260305004107.png]]
Далее получаем берём blkid (п2) и добавляем в /etc/fstab:
```
a2f59761-6861-4db1-b82f-2adc1346160d /mnt/newdisk ext4 noexec,noatime 0 2
```
![[images/Pasted image 20260305004606.png]]

Делаем reboot, убеждаемся, что наша ФС смонтировалась и пробуем запустить скрипт из этой ФС (работать не должно):
![[images/Pasted image 20260305004751.png]]
Ниче не работает. Берём не partuuid, а просто uuid + надо указать UUID=... вначале

![[images/Pasted image 20260305005410.png]]
После ребута система также монтируется.

Проверяем исполняемый файл (и время у файлов):
![[images/Pasted image 20260305005534.png]]

10. Увеличьте размер раздела и файловой системы до 1 Гб. Проверьте, что размер изменился:
Демонтируем систему и увеличиваем раздел через parted:
![[images/Pasted image 20260305182815.png]]

Теперь используем resize2fs:
![[images/Pasted image 20260305183316.png]] 
(гигабайт не получился, поэтому чуть поменьше сделаем)
![[images/Pasted image 20260305183349.png]]
![[images/Pasted image 20260305183422.png]]

11. Проверьте на наличие ошибок созданную файловую системы "в безопасном режиме", то есть в режиме запрета внесения каких-либо изменений в файловую систему, даже если обнаружены ошибки.
```bash
sudo fsck -t ext4 -CN /dev/vdb1
```
![[images/Pasted image 20260305184000.png]]

12. Создайте новый раздел, размером в 12 Мбайт. Настройте файловую систему, созданную в пункте 3 таким образом, чтобы ее журнал был расположен на разделе, созданном в этом пункте.

Создаем новый раздел:
```bash
sudo umount /dev/vdb1
sudo fdisk /dev/vdb
```
![[images/Pasted image 20260305185237.png]]

Создаём на новой партиции журнал, настраиваем первую партицию на этот журнал:
```bash
sudo mke2fs -O journal_dev /dev/vdb2
sudo tunde2fs -O ^has_journal /dev/vdb1
sudo tune2fs -J device=/dev/vdb2 /dev/vdb1
```
![[images/Pasted image 20260305191043.png]]


13. Создайте на 2 и 3-м добавленном диске разделы, занимающие весь диск. Инициализируйте для LVM все созданные разделы.

Создаём раздел на весь диск через fdisk:
![[images/Pasted image 20260306004612.png]]

Создаем Physical Volumes:
```bash
sudo pvcreate /dev/vdc1
sudo pvcreate /dev/vdd1
```
![[images/Pasted image 20260306004725.png]]

14. На дисках 2 и 3 создайте чередующийся LVM том и файловую систему ext4 на весь том.

Создаём Volume Group:
```bash
sudo vgcreate laba /dev/vdc1 /dev/vdd1
```
![[images/Pasted image 20260306005213.png]]

Создаём чередующийся (stripped) Logical Volume:
```bash
sudo lvcreate -i 2 -l 1022 --name laba-logical-volume laba
```
![[images/Pasted image 20260306010405.png]]

Делаем ФС:
```bash
sudo mkfs.ext4 /dev/laba/laba-logical-volume
```
![[images/Pasted image 20260306010603.png]]

15. Смонтируйте том в каталог /mnt/vol01 и настройте автомонтирование.
```bash
sudo mkdir -p /mnt/vol01
sudo vim /etc/fstab
# /dev/laba/laba-logical-volume /mnt/vol01 ext4 defaults 0 2
sudo mount -a
```
![[images/Pasted image 20260306012626.png]]


16. Получите информацию LVM о дисках, volume group и volume
![[images/Pasted image 20260306012950.png]]

17. Расширьте раздел на дополнительный диск используя ту же volume group, что и в п. 14. Расширьте том на 100% нового диска.

Делаем на новом диске раздел:
![[images/Pasted image 20260306013406.png]]

Добавляем в VG и расширяем:
![[images/Pasted image 20260306014059.png]]

Расширяем logical volume:
![[images/Pasted image 20260309125149.png]]

18. Расширьте файловую систему на 100% нового диска (обратите внимание, что вам не пришлось отмонтировать раздел)
![[images/Pasted image 20260309125255.png]]
И правда не пришлось :)

19. Получите информацию LVM о дисках, volume group и volume:
![[images/Pasted image 20260309125420.png]]


20. На машине server установите службу nfs-kernel-server, разрешите запуск и запустите ее. При необходимости разрешите доступ через сеть к этой службе:
```bash
sudo apt install nfs-kernel-server
sudo systemctl enable nfs-server
sudo systemctl start nfs-server
```
![[images/Pasted image 20260309131239.png]]

21. Сделайте так, чтобы к каталогу /mnt/vol01можно было получить доступ через NFS, при этом установите параметры, которые:
	1. Разрешают доступ к каталогу только с IP адресов сети ваших виртуальных машин.
	2. Разрешают монтировать каталог для записи.

```bash
# На сервере
sudo vim /etc/exports
# /mnt/vol01 192.168.65.0/24(rw,sync,no_subtree_check)
```
![[images/Pasted image 20260309141422.png]]

22. На компьютере client осуществите монтирование сетевого ресурса в каталог /var/remotenfs.
```bash
# На клиенте
showmount -e 192.168.65.6
sudo mkdir -p /var/remotenfs
sudo mount -t nfs 192.168.65.6:/mnt/vol01 /var/remotenfs
```
![[images/Pasted image 20260309142242.png]]

22. Убедитесь, что монтирование удалось. Скопируйте в каталог remotenfs любой файл.
```bash
# На клиенте
echo "some info" > text.txt
sudo cp text.txt /var/remotenfs/
```
![[images/Pasted image 20260309142525.png]]

Меняем права на сервере:
![[images/Pasted image 20260309142814.png]]

Теперь работает:
![[images/Pasted image 20260309142848.png]]
Проверяем на сервере:
![[images/Pasted image 20260309142913.png]]

23. Получите информацию о inode любого файла, выведите информацию о логическом размещении файла на диске
На сервере:
![[images/Pasted image 20260309143213.png]]

На клиенте:
![[images/Pasted image 20260309143313.png]]

24. Создайте мягкую и жесткую ссылки на файл, посмотрите информацию о их логическом расположении, inode, найдите отличия и сходства, чем они объясняются
```bash
echo "another info" > file.txt
ln -s file.txt symlink
ln file.txt hardlink
```
![[images/Pasted image 20260309150526.png]]

Интересные замечания:
- У симлинка свой Inode, у hardlink и файла – один
- У файла в поле links стоит 2. У файла две ссылки (обе равнозначны)
- У симлинка blocks = 0. Это механизм fast link, когда для коротких ссылок не выделяются блоки данных
