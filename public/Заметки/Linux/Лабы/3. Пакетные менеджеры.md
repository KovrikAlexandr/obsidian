1. Выведите список всех подключенных репозитариев
```bash
sudo apt policy
```
![[images/Pasted image 20260309191338.png]]


2. Обновите локальные индексы пакетов в менеджере пакетов.
```bash
sudo apt update
```
![[images/Pasted image 20260309191324.png]]


3. Выведите информацию о метапакете build-essential:
```bash
sudo apt show build-essential
```
![[images/Pasted image 20260309191611.png]]
- Сам пакет почти ничего не весит, зато тянет за собой зависимости для сборки deb пакетов

4. Установите метапакет build-essential, при этом определите какие компоненты будут установлены, а какие обновлены.
```bash
sudo apt depends build-essential
sudo apt install build-essential # Перед тем, как прожать Y, можно посмотреть, что будет установлено и обновлено
```
![[images/Pasted image 20260309192434.png]]


5. Найдите пакет, в описании которого присутствует строка «clone with a bastard algorithm»
```bash
sudo apt search "clone with a bastard algorithm"
```
![[images/Pasted image 20260309192602.png]]


6. Скачайте в отдельную директорию в домашнем каталоге архив с исходными кодами найденного в п.5 пакета.
Добавляем `deb-src` в репозитории:
```yml
Types: deb deb-src
URIs: http://ports.ubuntu.com/ubuntu-ports/
Suites: noble noble-updates noble-backports
Components: main restricted universe multiverse
Signed-By: /usr/share/keyrings/ubuntu-archive-keyring.gpg

Types: deb deb-src
URIs: http://ports.ubuntu.com/ubuntu-ports/
Suites: noble-security
Components: main restricted universe multiverse
Signed-By: /usr/share/keyrings/ubuntu-archive-keyring.gpg
```
Делаем update
![[images/Pasted image 20260309193055.png]]

Создаём папку и качаем исходники:
```bash
mkdir -p ~/bastet-src
cd ~/bastet-src
sudo apt source bastet
```
![[images/Pasted image 20260309193218.png]]


7. Установите пакет из исходных кодов, скаченных в п.6
Читаем инструкцию по сборке:
![[images/Pasted image 20260309193730.png]]
Ставим зависимости:
```bash
sudo apt install libboost-dev libncurses-dev
# Пришлось еще libboost-program-options-dev поставить, чтобы скомпилировалось
```

Собираем по инструкции:
```bash
make
cp bastet /usr/local/bin
chgrp games /usr/local/bin/bastet
chmod g+s /usr/local/bin/bastet
touch /var/games/bastet.scores2
chgrp games /var/games/bastet.scores2
chmod 664 /var/games/bastet.scores2
```
![[images/Pasted image 20260309195102.png]]

Рубимся в тетрис с ублюдским алгоритмом:
![[images/Pasted image 20260309195736.png]]


8. Если в конфигурационном файле пакета нет параметров установки пакета в систему, то добавьте их, так, чтобы пакет устанавливался в /usr/local/bin и на него назначались права ---rwxr-xr-x. Проверьте выполнения этих директив.

9. Проверьте, что любой пользователь может запускать установленный пакет, но не тратьте на это более 5 минут
Добавляем права и проверяем:
![[images/Pasted image 20260309195921.png]]


10. Создайте файл task10.log, в который выведите список всех установленных пакетов.
```bash
sudo apt list --installed > task10.log
```
![[images/Pasted image 20260309200116.png]]

11. Создайте файл task11.log, в который выведите список всех пакетов (зависимостей), необходимых для установки и работы компилятора gcc.
```bash
sudo apt-cache depends gcc | grep Depends | awk '{print $2}' > task11.log
```
![[images/Pasted image 20260309200442.png]]

12. Создайте файл task12.log, в который выведите список всех пакетов (зависимостей), установка которых требует установленного пакета libgpm2.
```bash
sudo apt-cache rdepends libgpm2 > task12.log
```
![[images/Pasted image 20260309200537.png]]

13. Создайте каталог localrepo в домашнем каталоге пользователя root и скопируйте в него c сайта http://snapshot.debian.org/package/htop/ пять разных версий пакета htop. Это можно сделать с помощью wget или просто передав файлы на виртуальную машину используя протокол ssh и утилиту scp.
```bash
mkdir -p /usr/localrepo
cd /usr/localrepo 
wget https://snapshot.debian.org/archive/debian/20250312T144852Z/pool/main/h/htop/htop_3.4.0-2_arm64.deb
wget https://snapshot.debian.org/archive/debian/20240113T150425Z/pool/main/h/htop/htop_3.3.0-2_arm64.deb
wget https://snapshot.debian.org/archive/debian/20220502T205642Z/pool/main/h/htop/htop_3.2.0-1_arm64.deb
wget https://snapshot.debian.org/archive/debian/20210115T083904Z/pool/main/h/htop/htop_3.0.5-2_arm64.deb
wget https://snapshot.debian.org/archive/debian/20200915T084655Z/pool/main/h/htop/htop_3.0.2-1_arm64.deb
```
![[Pasted image 20260310185905.png]]


14. Сгенерируйте в каталоге репозитория файл Packages, который будет содержать информацию о доступных пакетах в репозитории и файл Создайте файл Release, содержащий описание репозитория
Генерируем файлы Packages и Release:
```bash
dpkg-scanpackages --multiversion . /dev/null | gzip -9c | tee Packages.gz > /dev/null
apt-ftparchive release . > Release
```
![[Pasted image 20260314234455.png]]
- `Release` – содержит хеш суммы для проверки целостности
- `Packages` – содержит описания пакетов

Добавим репозиторий в apt:
```bash
vim /etc/apt/sources.list.d/local.list
# deb [trusted=yes] file:/root/localrepo ./
```

15. Обновите кэш apt
![[Pasted image 20260314235008.png]]

16. Выведите список подключенных репозиториев и краткую информацию о них.
```bash
apt policy
```
![[Pasted image 20260310193801.png]]

17. Создайте файл task16.log в который выведите список всех доступных версий htop
```bash
apt policy htop | tee task16.log
```
![[Pasted image 20260315163350.png]]

18. Установите предпоследнюю версию пакета (установил предпредпоследнюю, чтобы скачалось из локального репозитория)
![[Pasted image 20260315163432.png]]

19. Скачайте из сетевого репозитория пакет nano. Пересоберите пакет таким образом, чтобы после его установки, появлялась возможность запустить редактор nano из любого каталога, введя команду `newnano`. Для работы с пакетом следует использовать dpkg-deb, а для установки dpkg. В файле протокола работы опишите использованные команды.

Распаковываем deb пакет (на самом деле просто ar архив):
![[Pasted image 20260315164656.png]]

Ставим, пробуем:
![[Pasted image 20260315164938.png]]

20. Бонусный вопрос с подвохом - что есть в APT?
![[Pasted image 20260315163552.png]]