## Часть 1. Получение информацию о времени загрузки
1. Выведите информацию о времени, затраченном на загрузку системы:

```bash
systemd-analyze time
```
![[Pasted image 20260403232436.png]]

2. Выведите список всех запущенных при старте системы сервисов, в порядке уменьшения времени, затраченного на загрузку сервиса:

```bash
systemd-analyze blame
```
![[Pasted image 20260403232621.png]]

3. Выведите список сервисов, запуск которых с необходимостью предшествовал запуску сервиса sshd:

```bash
systemctl list-dependencies --after sshd
```
![[Pasted image 20260403232809.png]]

4. Сформируйте изображение в формате svg с графиком загрузки системы, сохраните его в файл:

```bash
systemd-analyze plot > plot.svg
```
![[Pasted image 20260403233236.png]]

## Часть 2. Управление юнитами
1. Получите список всех запущенных юнитов сервисов:

```bash
systemctl list-units --type=service --state=running
```
![[Pasted image 20260403233621.png]]

2. Выведите перечень всех юнитов сервисов, для которых назначена автозагрузка:

```bash
systemctl list-unit-files --type=service --state=enabled
```
![[Pasted image 20260403233719.png]]

3. Определите от каких юнитов зависит сервис sshd:

```bash
systemctl list-dependencies sshd
```
![[Pasted image 20260403233852.png]]

4. Определите запущен ли сервис cron, если нет, запустите его:

```bash
systemctl is-active cron
systemctl start cron
systemctl status cron
```
![[Pasted image 20260403234014.png]]

5. Выведите все параметры юнита cron, даже те, которые были назначены автоматически, и не были прописаны в файле юнита:

```bash
systemctl show cron
```
![[Pasted image 20260403234321.png]]

6. Запретите автозагрузку сервиса cron, но оставите ему возможность запускаться по зависимостям:

```bash
systemctl disable cron
```
![[Pasted image 20260403234350.png]]


## Часть 3. Создание сервиса
1. Создайте собственный сервис mymsg. Сервис mymsg должен:
	1. при старте системы записывать в системный журнал дату и время
	2. должен запускаться только если запущен сервис network.
```bash
vim /etc/systemd/system/mymsg.service
```

Конфигурация:
```
[Unit]
Requires=systemd-networkd.service
After=systemd-networkd.service

[Service]
Type=oneshot
ExecStart=/usr/bin/logger "date: $(date)"
RemainAfterExit=no

[Install]
WantedBy=multi-user.target
```

2. Настройте автоматический запуск сервиса mymsg при старте системы

```bash
systemctl enable mymsg.service
```

3. Запустите сервис.

```bash
systemctl start mymsg.service
```
![[Pasted image 20260403235342.png]]


## Часть 4. Работа с системным журналом
1. Выведите на консоль системный журнал. Убедитесь, что сервис mymsg
отработал корректно

2. Выведите на консоль все сообщения системного журнала, касающиеся
сервиса mymsg:

```bash
systemctl restart mymsg.service
journalctl -u mymsg.service
```
![[Pasted image 20260404001702.png]]

3. Выведите на экран все сообщения об ошибках в журнале:

```bash
journalctl -p err
```
![[Pasted image 20260404001926.png]]

4. Определите размер журнала:

```bash
journalctl --disk-usage
```
![[Pasted image 20260404001951.png]]


## Часть 5. Создание и настройка .mount юнита
1. Подготовьте файловую систему:
	1. Создайте новый раздел на диске или используйте существующий.
	2. отформатируйте его в файловую систему ext4.
	3. Создайте директорию для монтирования /mnt/mydata

```bash
fdisk /dev/vdb # g + n + w
mkfs.ext4 /dev/vdb1
```
![[Pasted image 20260404002531.png]]

2. Создание .mount юнита
	1. Создайте файл .mount юнита в /etc/systemd/system/mnt-mydata.mount.
	2. Настройте юнит следующим образом:
		1. Добавьте описание юнита в секцию Unit.
		2. В секции Mount укажите устройство, точку монтирования, тип файловой системы и опции.
		3. В секции Install укажите, что юнит должен быть активирован при достижении multi-user.target.

Конфигурация:
```
[Unit]

[Mount]
What=/dev/vdb1
Where=/mnt/mydata
Type=auto
Options=defaults

[Install]
WantedBy=multi-user.target
```

3. Запуск и проверка .mount юнита
```bash
systemctl daemon-reload  
systemctl enable mnt-mydata.mount  
systemctl start mnt-mydata.mount
systemctl status mnt-mydata.mount
```
![[Pasted image 20260404002941.png]]

## Часть 6. Использование .automount для отложенного монтирования

1. Подготовьте соответствующий .mount-юнит
	1. После выполнения Части 5 у вас должен был остаться юнит для монтирования /mnt/mydata
	2. Убедитесь, что при остановке раздел отмонтируется, а монтируется обратно только при запуске юнита или перезагрузке системы
![[Pasted image 20260404010306.png]]


2. Создание .automount юнита
	1. Создайте файл .automount юнита в /etc/systemd/system/mnt-mydata.automount.
	2. Настройте юнит:
		1. В секции Unit добавьте описание.
		2. В секции Automount укажите точку монтирования и время до размонтирования (TimeoutIdleSec).
		3. В секции Install укажите, что юнит должен быть активирован при достижении multi-user.target.

```bash
vim /etc/systemd/system/mnt-mydata.automount
```

Конфигурация:
```
[Unit]

[Automount]
Where=/mnt/mydata
TimeoutIdleSec=60

[Install]
WantedBy=multi-user.target
```

3. Запуск и проверка .automount юнита
	1. Включите и запустите .automount юнит.
	2. Проверьте статус юнита и убедитесь, что раздел монтируется при обращении к точке монтирования.
	3. Убедитесь, что раздел размонтируется после завершения работы.

```bash
systemctl daemon-reload  
systemctl enable mnt-mydata.automount  
systemctl start mnt-mydata.automount
```

![[Pasted image 20260404010819.png]]

autofs – механизм, при котором система монтируется on-demand

## Вопросы и задания:
1. Чем отличаются команды systemctl restart и systemctl try-restart?
	try-restart – This does nothing if units are not running.

	restart – If the units are not running yet, they will be started.

Это из man


2. Как с помощью systemctl запустить Linux в однопользовательском
режиме?

```bash
systemctl rescue
```

3. Пусть вам нужно создать еще один сервис mysrv, который:
	- не будет запускаться автоматически
	- может быть выполнен, только если сервис mymsg будет принудительно остановлен уже после старта системы. 

Приведите параметры и их значения из описания юнитов mymsg и mysrv, которые обеспечат выполнения этих условий.

В mysrv:
```ini
[Unit]
ConditionPathExists=!/run/mymsg.pid
```

mymsg.service будет оставлять pid в директории /run. При остановке (ExecStop он будет его удалять)

mysrv сможет запуститься только, если этого файла нет (то есть, если mymsg не работает)

Для отключения автозапуска оставляем Install пустым