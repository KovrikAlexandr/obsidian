Создать скрипт, который:
1. Выводит в файл work3.log построчно список всех пользователей в системе в следующем формате: «user NNN has id MM»:
	```bash
	awk -F ':' '{print "User", $1, "has", "ID", $3}' /etc/passwd | tee --append work3.log
	```
![[Pasted image 20260228194425.png]]

2. Добавляет в файл work3.log строку, содержащую дату последней смены пароля для пользователя root:
	```bash
	sudo awk -F':' '$1 == "root" {print $3 * 86400}' /etc/shadow | xargs -I{} \
	date -d "@{}" +%Y-%m-%d | tee --append work3.log
	```
![[Pasted image 20260228194626.png]]

3. Добавляет в файл work3.log список всех групп в системе (только названия групп) через запятую:
	```bash
	sudo awk -F':' '{print $1}' /etc/group | tr '\n' ',' | tee --append work3.log
	echo '' | tee --append work3.log # For new line character
	```
![[images/Pasted image 20260228194619.png]]

4. Делает так, чтобы при создании нового пользователя у него в домашнем каталоге создавался файл readme.txt с текстом «Be careful!»:
	```bash
	echo "Be careful!" | sudo tee /etc/skel/readme.txt
	```
![[Pasted image 20260228194656.png]]

5. создает пользователя u1 с паролем 12345678:
	```bash
	sudo useradd -p 12345678 u1
	```

6. создает группу g1:
	```bash
	sudo groupadd g1
	```

7. Делает так, чтобы пользователь u1 дополнительно входил в группу g1:
	```bash
	sudo usermod -aG g1 u1
	```

8. Добавляет в файл work3.log строку, содержащую сведения об идентификаторе и имени пользователя u1 и идентификаторах и именах всех групп, в которые он входит
	```bash
	id u1 | tee --append work3.log
	```

9. Делает так, чтобы пользователь `myuser` дополнительно входил в группу g1
	```bash
	sudo useradd myuser
	sudo usermod -aG g1 myuser
	```

10. Добавляет в файл work3.log строку с перечнем пользователей в группе g1 через запятую
	```bash
	cat /etc/group | awk -F':' '$1 == "g1" {print $4}'
	```

11. Делает так, что при входе пользователя u1 в систему вместо оболочки bash автоматически запускается /usr/bin/mc, при выходе из которого пользователь возвращается к вводу логина и пароля
	```bash
	sudo usermod -s /usr/bin/mc u1
	cat /etc/passwd | grep u1
	```

12. создает пользователя u2 с паролем 87654321
	```bash
	sudo useradd -p 87654321 u2
	```
![[Pasted image 20260228195235.png]]

13. в каталоге /home создает каталог test13, в который копирует файл work3.log два раза с разными именами (work3-1.log и work3-2.log)
	```bash
	sudo mkdir -p /home/test13
	sudo cp work3.log /home/test13/work3-1.log
	sudo cp work3.log /home/test13/work3-2.log
	```
![[Pasted image 20260228195421.png]]

14. сделает так, чтобы пользователи u1 и u2 могли просматривать каталог test13 и читать файлы, только пользователь u1 смог изменять и удалять их, а все остальные пользователи системы не могли просматривать содержимое каталога test13 и файлов в нем. При этом никто не должен иметь права исполнять эти файлы
	```bash
	sudo chown -R u1:u2 /home/test13
	sudo chmod -R 640 /home/test13
	sudo chmod 750 /home/test13
	```
	![[Pasted image 20260228195554.png]]
	- Проверяем юзера `u1`:
	![[Pasted image 20260228195925.png]]
	- Проверяем `u2`:
	![[Pasted image 20260228195951.png]]
	Проверяем other (например myuser):
	![[Pasted image 20260228200122.png]]

15. создает в каталоге /home каталог test14, в который любой пользователь системы сможет записать данные, но удалить любой файл сможет только пользователь, который его создал или пользователь u1
	```bash
	sudo mkdir -p /home/test14
	sudo chown root:root /home/test14
	sudo chmod 1777 /home/test14
	```
	![[Pasted image 20260228202028.png]]

16. копирует в каталог test14 исполняемый файл редактора nano и делает так, чтобы любой пользователь смог изменять с его помощью файлы, созданные в пункте 13
	```bash
	sudo cp $(which nano) /home/test14/
	sudo chown u1:u1 /home/test14/nano
	sudo chmod 4777 /home/test14/nano
	```
	![[Pasted image 20260228203054.png]]

17. создает каталог test15 и создает в нем текстовый файл /test15/secret_file. Делает так, чтоб содержимое этого файла можно было вывести на экран, только зная имя файла, но узнать имена файлов в каталоге кроме как подбором было бы невозможно.
	```bash
	sudo mkdir -p /home/test15
	sudo chmod 711 /home/test15
	sudo touch /home/test15/secret_file
	sudo chmod 777 /home/test15/secret_file
	```
	![[Pasted image 20260228230042.png]]

18. Настроить sudo таким образом, чтобы пользователь u1 смог с помощью sudo и команды passwd менять пароли другим пользователям, но не смог бы использовать другие утилиты от имени root:
	```bash
	sudo visudo
	# u1 ALL = (root) /usr/bin/passwd
	```
	![[Pasted image 20260228233900.png]]


Обратный скрипт:
```bash
sudo rm -rf \
work3.log \
/etc/skel/readme.txt \
/home/test13 \
/home/test15 \
/home/test14

sudo userdel -rf u1 2> /dev/null
sudo userdel -rf u2 2> /dev/null
sudo userdel -rf myuser 2> /dev/null
sudo groupdel g1 2> /dev/null
```