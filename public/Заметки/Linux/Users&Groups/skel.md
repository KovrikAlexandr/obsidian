При создании нового пользователя содержимое skel копируется в его home.

Можно поменять путь к каталого-шаблону в файле `/etc/default/useradd`. По умолчанию настройка такая:
```bash
SKEL=/etc/skel
```
