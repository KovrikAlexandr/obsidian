В файле `/etc/group` хранится базовая информация о группах.
Одна строка – одна группа.
Формат:
```
name:x:GID:user1,user2,user3
```
- name – имя группы
- `x` – значит, что пароль хранится в `/etc/gshadow`
- `GID` – идентификатор
- Далее идёт список участников группы через запятую