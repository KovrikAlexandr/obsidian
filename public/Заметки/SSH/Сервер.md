Основной юнит:
```bash
systemctl status ssh.service # Alias=sshd.service
```

Основной конфиг:
```bash
vim /etc/ssh/sshd_config
```

Сейчас порт слушает другой юнит:
```bash
systemctl status ssh.socket
```

Для смены порта можно поменять конфиг `/etc/ssh/sshd_config`